# Supertrend 优化记录

## 当前研究基线

2026-07-18 起，策略重新收敛为可解释的 Supertrend 基线：

```text
3m Supertrend 翻转或回踩收回
  -> 大周期背景过滤
  -> 趋势、均线、MACD、量价和风险过滤
  -> 全仓开仓
```

Supertrend 是唯一信号源，其他信息只能过滤或记录。历史 SMC、shock 和 intraday 独立入口实验已经删除。

## 2026-07-19 研究收敛结论

当前可复现的单持仓研究基线固定为：初始保证金 `10000 USDT`、单笔保证金 `100 USDT`、`100x` 杠杆、初始止损 `50 bps`、盈利 `50 bps` 后抬至 `+16 bps`、盈利 `75 bps` 后按 `30 bps` 回撤全仓退出，往返手续费与滑点合计 `16 bps`。研究比较标准、自适应和 AI 三种 Supertrend 的原始 `flip`，不把实验性延续事件称为正式策略信号。

ETHUSDT 2025-08 的基线结果为：标准版 `38` 笔、`-424.09 USDT`、PF `0.643`；自适应版 `38` 笔、`-413.78 USDT`、PF `0.652`；AI 版 `15` 笔、`+19.68 USDT`、PF `1.050`。AI 只是小样本微利，尚不能证明跨月正期望；标准和自适应在该高波动月份明显亏损。

逐笔诊断确认，基线亏损主要是未达到保本门槛前触发初始止损，不是大幅盈利后的回吐。标准版有多笔盈利退出后继续运行，说明退出存在提前兑现问题，但把全仓跟踪参数放宽到 `100/40` 或 `150/60` 后只改善8月，2025-09至11月跨月结果恶化，属于行情依赖，不保留。

以下增加交易次数的实验已判定失败并从代码删除：同趋势结构重入、独立3m BOS立即/次根确认、5m BOS后3m回踩收复。它们虽能增加成交，但新增交易为负期望。AI候选因子上限从 `1-5` 缩到 `1-4` 或 `1-3.5` 能让首次翻转更早、覆盖更多波段，却显著增加震荡翻转；两档PF分别降至 `0.259` 和 `0.454`，同样删除。

因此当前不再把“提高单一标的单月订单数”作为优化目标。后续只有在全部 AI flip 的统一前向标签中找到预先定义、跨窗口稳定的被拦截正期望子集，才允许增加入口；否则保持原始 AI `1-5` 的选择性。

### 2025-08 至 2025-11 波段覆盖与场景组合收敛

以 ETHUSDT 3m、绝对波幅至少 `30 USDT`、反向 `10 USDT` 确认拐点进行事后机会审计。该口径只用于复盘，不是实时信号。2025-09、10、11 月分别识别 `482`、`881`、`739` 个波段；其中至少 `60 USDT` 的中大型波段合计 `294` 个，附近存在 AI flip 的只有 `59` 个。漏失中 `126` 个属于 AI 已同向但没有新的延续授权，`63` 个属于应翻转但没有 AI flip。现有 flip 对中大型波段的覆盖约 `20.1%`，真正处于启动前或波段前 20% 的覆盖约 `9.5%`。

为增加单量曾用同一 v6 市场状态、仓位、成本和退出口径比较 AI flip、`trend_pullback_resume`、平台/压缩突破、`volatility_impulse` 及组合。2025-08 单月中回撤恢复为 `29` 笔、`+294.70 USDT`、PF `1.496`，突破为 `43` 笔、`+243.76 USDT`、PF `1.284`；但回撤恢复在 2025-09、10、11 月分别为 `-588.40 / -370.09 / -424.34 USDT`，PF 分别为 `0.575 / 0.569 / 0.416`。突破在 2025-10、11 月分别为 `-576.24 / -653.60 USDT`，PF 为 `0.603 / 0.293`。脉冲入口各月持续负期望，四场景全集和逐项消融也未形成跨月稳定组合。因此这些场景不得接入正式策略，相关组合回放实现已删除。

初始止损诊断对四类独立研究入口获得 `244` 个样本：`130` 个在止损后 20 根 3m K 线内继续反向运行至少 30 点，归为方向识别错误；`41` 个为脉冲无延续，`31` 个为假突破，`16` 个为回撤恢复失败；只有 `17` 个在止损后重新顺向运行至少 30 点，可能属于止损偏紧或入场时机不佳。主要问题不是统一止损过紧，不能通过全面放宽止损解决。

趋势年龄、本趋势内第几次场景、5m/15m 同向以及多周期 MACD 动量同步也未形成稳定区分。盈利与方向错误样本的首次场景占比分别为 `87.4% / 87.7%`，5m 与 15m 同时同向占比为 `87.8% / 89.2%`，至少一个高周期动量同步占比为 `96.1% / 94.6%`；趋势年龄和累计位移的差异在月份间反转。相关生命周期过滤实验已删除，不得继续在 2025-09 至 11 月扫描阈值后将结果称为验证。

候选出现后的固定早期确认也已完成否证。保持 v6 市场状态、仓位、成本与退出不变，对回撤恢复和突破候选分别测试立即开仓、下一根方向收盘、连续两根保持、突破后回踩重获。下一根确认在各月均明显恶化；连续两根仅 2025-09 回撤恢复微利 `+10.48 USDT`、PF `1.027`，其余组合仍为负。回撤后重获仅 2025-10 有效（`+394.63 USDT`、PF `2.495`），另外三个月全部亏损；突破回踩在 2025-08、09 分别亏损 `-494.92 / -179.59 USDT`，2025-10、11 才盈利 `+373.84 / +142.73 USDT`，四个月合计仍亏损约 `-157.94 USDT`。固定等待确认没有形成跨月稳定优势，相关实现已删除，也不得继续在同一月份扫描等待根数或回踩窗口。

下一步若继续提高有效覆盖，应先研究“候选是否属于可交易行情”的实时状态特征，并使用未参与规则形成的新冻结窗口验证；不能再从 2025-08 至 11 月反推确认阈值。

## 已确认的历史问题

- 允许 SMC BOS 独立触发时，交易序列高度集中于单一辅助信号，策略不再具有 Supertrend 可解释性。
- 放宽 shock 或日内脉冲入口会显著增加交易和手续费，但没有形成稳定正期望。
- 多个方向指标累加评分会重复表达相同趋势状态，旧评分没有观察到稳定的收益排序能力。
- 入口和退出必须继续采用单变量实验，不能同时修改。

旧回测结果、ClickHouse Run 和临时报告已经清理。它们只用于促成当前语义修正，不再作为现版本收益依据。

## 下一阶段研究闭环

未来一段时间停止围绕单月收益继续扫描 Supertrend 参数，把目标收缩为：先建立客观的离线行情标签，再回答 Supertrend 原始信号在什么市场条件下具有跨月稳定的正期望。实施必须按以下阶段顺序推进；前一阶段没有完成验收时，不进入后一阶段。

### 阶段一：离线行情标签

统一生成互斥的六类结果侧标签：`trend_up`、`trend_down`、`high_volatility_range`、`low_volatility_consolidation`、`trend_start`、`trend_exhaustion`。标签协议版本固定为 `forward-market-label.v1`，首版观察窗口固定为未来 `20/40/80` 根 3m K 线。

每个观察窗口先保存连续路径指标，再生成离散标签。连续指标至少包括：方向收益、半程收益、最大上涨和下跌波动、路径效率、已实现波动率、收盘路径最大回撤和恢复、主导波动方向、主导波动出现位置及其最终回吐。K 线高低价只用于最大有利和不利波动；回撤、恢复和路径效率使用收盘序列，避免假设同一根 K 线内高低点的先后顺序。

路径指标使用以下稳定派生语义：`late_return = final_return - midpoint_return`；`phase_expansion = abs(late_return) / max(abs(midpoint_return), epsilon)`；`directional_advantage = (max_upside - max_downside) / (max_upside + max_downside)`；`dominant_giveback_ratio = dominant_giveback / dominant_excursion`；`dominant_retention` 表示窗口结束时仍保留的主导方向收益占主导波动的比例；`dominant_excursion_position` 是主导极值首次出现根数除以窗口根数。这些连续指标保留原值，不因最终标签而覆盖或裁剪。

六类标签固定按以下优先级互斥判定：

1. `trend_exhaustion`：主导波动足够大、极值主要出现在前半段、随后明显回吐、最终保留比例较低，且不再满足普通趋势的路径质量；
2. `trend_start`：前半段方向收益较小、后半段显著扩张、主导极值出现在后半段、最终方向与主导方向一致、回吐较低且路径效率达到趋势要求；
3. `trend_up` / `trend_down`：最终方向收益、路径效率、方向优势和保留比例达到普通趋势要求，并按最终收益正负确定方向；
4. `high_volatility_range`：未命中趋势类标签，已实现波动率或双向总波幅较高，但最终方向收益、路径效率或方向优势不足；
5. `low_volatility_consolidation`：其余不满足趋势和高波动震荡条件的有效样本。

具体数值阈值尚未冻结。每个 `symbol + interval + horizon_bars` 必须在训练窗口独立统计方向收益、路径效率、波动率、主导波动、方向优势和回吐比例的分布，再一次性评审并冻结阈值。冻结配置至少保存标签版本、标的、周期、观察根数、训练起止时间、全部阈值、判定优先级和创建时间。样本外运行只能读取冻结配置，不得重新计算分位数，也不得为了平衡六类样本量反复调整阈值。

`forward_market_label` 是使用未来行情生成的离线研究目标；现有 `market_regime` 是只使用当时可见数据的实时状态。两者必须在类型、字段、存储和调用路径上保持隔离，`forward_market_label` 不得进入在线 snapshot、策略评分、仓位授权或执行服务。

验收条件：相同输入可重复生成相同结果；路径不足、未闭合 K 线、无效价格、时间倒序和 K 线间隔不连续时显式失败；20/40/80 三个窗口分别独立生成，不互相投票或修正；标签不按 Supertrend 买卖方向归一化；无在线消费者可以读取离线标签。

### 阶段二：信号条件优势

对全部 Supertrend 原始 flip 独立研究，包括策略已放行和被当前市场状态拦截的信号，不以实际订单样本代替原始信号总体。预先声明的分组维度为：

- `signal_version`
- `forward_market_label`
- `direction`
- `volatility_bucket`
- `trend_strength_bucket`
- `distance_from_ma_bucket`
- `multi_timeframe_alignment`
- `session`
- `month`

每组统一输出样本量、胜率、平均及中位 MFE/MAE、平均盈利、平均亏损、盈亏比、扣除手续费和滑点后的每信号期望、Profit Factor、95% 置信区间以及逐月结果。胜率必须同时声明止盈止损和持有期定义；净期望的成本模型必须随报告保存。小样本分组应明确标记为不足，不参与策略晋级。

95% 置信区间优先对每信号净收益使用 bootstrap，对胜率使用 Wilson 区间。分组和候选条件必须在查看冻结窗口前声明，禁止根据单月盈利样本反推阈值。候选条件首先要在独立信号结果中形成正净期望，再进入单持仓顺序回放验证成本、PF 和回撤；增加订单数本身不是验收条件。

验收条件：训练区间逐月方向一致；冻结窗口仍为正净期望且置信区间没有显示优势完全依赖少数异常样本；样本量、最差月份和正期望月份占比全部随结论披露。

### 阶段三：Regime、Setup、Trigger 分层

只有阶段二发现冻结窗口稳定优势后，才调整正式策略。职责固定为：

- Regime：只输出允许做多、允许做空、双向或禁止交易；
- Setup：描述值得关注的回撤、压缩、结构位置或其他候选形态；
- Trigger：决定具体入场时机，Supertrend flip 仍是当前唯一正式方向授权事件。

每层必须有独立版本、输出和原因字段。相关指标按趋势、波动、结构、位置和成交量来源族群去重，禁止让同一底层方向信息同时参与市场判断、入场触发、多周期确认和风控授权。固定入口后再单独研究退出；不得在同一实验中同时修改入口和退出。1m 最后才用于信号出现后的执行择时，不参与策略方向判断。

验收条件：实时与回测消费同一份可见特征；移除任一层时可以完成独立消融；不存在离线标签泄漏；年度、重点行情、逐月和样本外结果均可复现。

### 阶段四：paper 连续运行与对账

研究达到稳定正期望后仍只进入 paper。连续记录实时原始信号、策略授权、订单意图、模拟成交、行情时间和处理时间，并与相同时间段的历史回放逐条对账，统计信号缺失、方向差异、时间偏差、价格偏差、滑点、延迟和费用差异。

testnet/live 继续冻结。只有 paper 对账稳定、策略达到冻结窗口正期望、真实凭证联调完成、小额订单验收通过且账户级风控就绪后，才能另行评审是否进入 testnet 或小额 live；研究计划本身不构成实盘授权。

### 当前进度与实施边界

- 已建立 `forward-market-label.v1` 的六类标签枚举、20/40/80 窗口、连续路径指标和互斥判定优先级，并完成 `pkg/signalresearch` 单元测试；具体分类阈值尚未冻结。
- 已新增只读 `forward-market-label-research` 命令。命令只读取 ClickHouse K 线并输出 JSON，不初始化研究 Schema、不写 ClickHouse，也不接入在线策略。
- 阈值训练区间固定为 `2024-08-01T00:00:00Z` 至 `2024-11-01T00:00:00Z`，包含 44,160 个观察点；20/40/80 三个窗口各有 44,160 个有效样本和 0 个无效样本。报告使用 nearest-rank 输出总体及逐月 P10/P20/P25/P40/P50/P60/P75/P80/P90、均值、中位数和极值。
- 训练分布已经补齐绝对方向收益、绝对半程/后半程收益、绝对方向优势、双向总波幅，以及前后半程同向率。相邻观察点路径高度重叠，44,160 不能当作独立统计样本量，后续置信区间必须按时间块抽样。
- 冻结验证区间固定为 `2024-11-01T00:00:00Z` 至 `2025-01-01T00:00:00Z`，当前尚未运行或查看。首版阈值必须只根据训练分布提出、评审并固化后，才能读取冻结验证区间。
- 下一步只评审 20/40/80 三组数值阈值和训练期总体/逐月标签占比，不修改在线策略、不把训练分布解释为策略收益。
- 阈值冻结并通过验证后，再接入信号条件优势研究；ClickHouse 新表或字段属于数据库修改，必须单独确认。
- 条件优势报告通过前不开始 Regime/Setup/Trigger 策略改造。
- 所有版本在完成年度、重点行情、逐月稳定性和样本外验证前均不能进入实盘。

## 市场特征与事件基础设施计划

后续优化优先建设可复用的市场能力层，不继续在策略内部堆叠指标判断。数据链路统一为：

```text
K线与成交数据
  -> 连续特征 Feature
  -> 市场状态 State
  -> 市场事件 Event
  -> 质量评估 Quality
  -> 策略授权与执行 Strategy
```

基础设施只描述市场，不产生买卖决策。Supertrend 继续作为当前策略唯一的方向授权事件；其他特征、状态和事件只能参与质量评估、过滤、风险控制或离线研究。

### 能力域

| 能力 | 连续特征 | 状态与事件 |
| --- | --- | --- |
| 趋势 | 方向、ADX、DI 差、斜率、持续时间、趋势效率 | 形成、延续、成熟、衰减、冲突 |
| 波动 | ATR 百分比、ATR 分位数、波幅变化率、带宽 | 收缩、正常、扩张、异常、冲击 |
| 结构 | 摆动高低点、HH/HL、LH/LL、区间边界 | BOS、CHoCH、结构延续、结构失效 |
| 位置 | 均线 ATR 距离、布林位置、VWAP 偏离、摆动点距离 | 回撤区、价值区、区间边缘、过度延伸 |
| 事件 | 穿越幅度、突破幅度、回踩距离、影线收回 | 翻转、突破、回踩、reclaim、假突破、均线交叉 |
| 质量 | 年龄、确认数、连续性、冲突数、来源族群 | 新鲜度、可靠度、拥挤度、冲突度 |

### 分层语义

连续值、离散状态和瞬时事件必须分开。例如 `adx14=28.6` 是 Feature，`trend_strength=strengthening` 是 State，`trend_strength_confirmed` 才是 Event。策略消费稳定语义，不直接重复解释一组原始指标。

每项基础能力至少应声明：

- 稳定字段名、类型、单位和计算周期；
- 当前值、前值、变化量、斜率或分位数；
- 数据有效性、口径版本和计算时间；
- 来源族群、方向、首次发生时间、年龄和失效状态；
- 是否严格只使用当时可见数据。

### 实施阶段

1. **能力审计与数据字典**：盘点现有指标输出、消费者、单位、窗口、缺失条件、重复语义和未来数据风险。
2. **统一特征协议**：在现有 `ValueSet`、数值窗口和信号窗口上增量扩展，不先重构指标模块。
3. **市场状态层**：基于历史分布生成趋势、波动、结构和位置状态，避免凭单次回测硬编码阈值。
4. **事件生命周期**：统一记录方向、幅度、发生时间、年龄、确认数、失效状态和来源。
5. **质量与冲突层**：按趋势、波动、位置、结构和成交量族群限制重复贡献，避免相关指标重复计分。
6. **研究与标签层**：统一生成 30/60/120 分钟方向收益、MFE、MAE、最大回吐和 first-passage 标签。
7. **策略接入**：只有通过按月及样本外稳定性验证的能力才能进入评分；仍不得绕过 Supertrend 独立授权交易。

### 当前数据结论

2025-09-01 至 2025-12-01 的 ETHUSDT 3m 候选信号研究表明，多数指标存在非线性甜区，不能采用“越强越好”的评分方式：

- 中等 ATR 百分比的方向收益和路径质量优于极端波动；
- Supertrend 距离、STC 变化和 VWAP 偏离过大时，信号更接近过度扩张或趋势末端；
- ADX 和 EMA 斜率极高不能直接视为高质量入口；
- 评分应拆分为 `direction_score`、`entry_quality_score` 和 `overextension_penalty`。

当前 `entry_feature_snapshot` 已随 Supertrend 候选事件记录已有趋势、波动、位置和动量特征，用于离线研究；该诊断不参与策略评分或过滤。

### 验证边界

- 实时、回测和研究必须使用同一特征口径；
- 不因单次策略盈亏临时修改基础字段含义；
- 先验证底层数据的区分能力，再评估策略收益；
- 训练窗口内完成按月稳定性检验后，必须更换时间段做样本外交叉验证；
- 1m 仅在 3m 信号出现后用于执行择时，不参与策略方向判断。

## ETH Supertrend 首触止盈止损复盘

2026-07-18 对 ETHUSDT 3m Supertrend 翻转信号进行了逐信号 first-passage 复盘。研究覆盖三个互不重叠的季度窗口：2025-01 至 2025-04、2025-09 至 2025-12、2026-01 至 2026-04。

### 统计口径

- 原始样本为 3,273 个 Supertrend 翻转候选；每个候选独立模拟，允许持仓时间重叠。
- 当前策略最终放行 588 个信号。原始候选与当前放行信号分别统计，不能混称为实际订单。
- 信号 K 线收盘价为参考入场价，从下一根 3m K 线开始判断。
- 每个信号只产生一个结果：首次止盈、首次止损或 12 小时到期平仓。
- 同一根 3m K 线同时覆盖止盈和止损价格时，保守地按止损处理。
- 100 倍杠杆下，表内 30% 收益目标对应 30 bps，即约 0.30% 的标的价格变动。
- 每笔往返手续费和滑点合计按 16 bps 计算，约等于 16% 保证金收益。

下列表格每格为 `平均每信号净收益 / 三段样本最低 Profit Factor`。平均净收益单位是 100 倍杠杆下的保证金百分比，已扣除 16% 往返成本。

### 原始 Supertrend 候选参数表

| 止盈 \ 止损 | 50% | 70% | 90% | 100% | 120% | 150% |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 30% | -15.8 / 0.33 | -15.7 / 0.36 | -15.7 / 0.38 | -16.1 / 0.38 | -16.7 / 0.39 | -16.3 / 0.40 |
| 50% | -15.3 / 0.50 | -15.3 / 0.53 | -15.6 / 0.56 | -15.9 / 0.57 | -16.3 / 0.58 | -16.9 / 0.56 |
| 75% | -14.6 / 0.58 | -14.3 / 0.61 | -15.2 / 0.66 | -15.7 / 0.67 | -15.7 / 0.67 | -15.9 / 0.68 |
| 100% | -14.3 / 0.62 | -13.7 / 0.67 | -14.9 / 0.71 | -15.3 / 0.71 | -15.3 / 0.73 | -15.3 / 0.73 |
| 150% | -13.7 / 0.64 | -12.7 / 0.73 | -14.9 / 0.72 | -15.1 / 0.72 | -15.3 / 0.73 | -16.3 / 0.74 |
| 200% | -13.6 / 0.66 | -12.2 / 0.75 | -14.9 / 0.73 | -15.1 / 0.73 | -15.1 / 0.73 | -16.0 / 0.75 |
| 250% | -14.1 / 0.68 | -12.8 / 0.76 | -15.4 / 0.74 | -15.8 / 0.74 | -16.3 / 0.73 | -17.1 / 0.75 |
| 300% | -13.9 / 0.69 | -12.7 / 0.78 | -15.5 / 0.75 | -16.0 / 0.74 | -16.3 / 0.75 | -16.5 / 0.77 |
| 500% | -11.3 / 0.74 | -9.6 / 0.82 | -12.1 / 0.79 | -12.4 / 0.79 | -13.1 / 0.77 | -12.8 / 0.80 |

原始候选没有任何一组参数同时满足三段样本净收益为正且 Profit Factor 大于 1。相对最优的 500% / 70% 仍为每信号 -9.62%，综合 Profit Factor 0.844。

盈利机会本身并不少。较低止盈目标具有较高的首次命中率，但无法覆盖止损赔率和固定成本：

| 止盈 / 止损 | 止盈 | 止损 | 到期 | 最终正收益 | 正收益率 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 30% / 70% | 2,291 | 961 | 21 | 2,291 | 70.0% |
| 30% / 150% | 2,669 | 512 | 92 | 2,671 | 81.6% |
| 50% / 70% | 1,906 | 1,324 | 43 | 1,914 | 58.5% |
| 75% / 100% | 1,788 | 1,317 | 168 | 1,835 | 56.1% |
| 100% / 100% | 1,525 | 1,503 | 245 | 1,613 | 49.3% |
| 150% / 70% | 969 | 2,037 | 267 | 1,135 | 34.7% |
| 500% / 70% | 169 | 2,294 | 810 | 831 | 25.4% |

例如 30% / 150% 的正收益率达到 81.6%，但扣除成本后单次止盈约为 +14%，单次止损约为 -166%，高胜率仍不能形成正期望。因此后续指标筛选必须以净期望和 Profit Factor 为主，不能只优化胜率。

### 当前策略放行信号参数表

| 止盈 \ 止损 | 50% | 70% | 90% | 100% | 120% | 150% |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 30% | -14.8 / 0.37 | -12.8 / 0.40 | -13.5 / 0.37 | -14.4 / 0.36 | -14.3 / 0.38 | -13.0 / 0.39 |
| 50% | -13.0 / 0.52 | -10.8 / 0.58 | -10.8 / 0.55 | -12.0 / 0.53 | -11.1 / 0.59 | -9.9 / 0.61 |
| 75% | -12.8 / 0.58 | -10.1 / 0.70 | -11.0 / 0.72 | -11.8 / 0.69 | -12.5 / 0.71 | -11.3 / 0.69 |
| 100% | -13.6 / 0.61 | -10.3 / 0.74 | -12.2 / 0.73 | -12.6 / 0.72 | -13.5 / 0.74 | -14.5 / 0.70 |
| 150% | -13.8 / 0.63 | -10.2 / 0.79 | -14.1 / 0.76 | -14.6 / 0.74 | -13.4 / 0.74 | -14.5 / 0.73 |
| 200% | -14.1 / 0.65 | -9.2 / 0.82 | -13.6 / 0.78 | -14.2 / 0.75 | -13.6 / 0.78 | -14.0 / 0.78 |
| 250% | -15.5 / 0.64 | -10.2 / 0.72 | -15.6 / 0.68 | -16.0 / 0.66 | -16.2 / 0.68 | -17.6 / 0.68 |
| 300% | -14.8 / 0.68 | -9.6 / 0.72 | -15.1 / 0.67 | -15.9 / 0.64 | -16.0 / 0.63 | -16.7 / 0.65 |
| 500% | -12.5 / 0.63 | -6.0 / 0.67 | -11.3 / 0.63 | -11.9 / 0.61 | -12.8 / 0.57 | -13.9 / 0.58 |

当前过滤相对原始候选有一定改善，但 54 组参数仍全部亏损。按跨窗口最差表现排序，当前放行信号相对较好的参数是 200% / 70%：588 个信号平均 -9.16%，综合 Profit Factor 0.841，三段样本平均分别为 -10.73%、-7.04%、-9.52%。现有评分没有找到足以覆盖成本的信号子集。

### 指标有效性分析

指标使用方向归一化值：买入方向保持原值，卖出方向对有方向的距离和动量取反。例如 `dynamic_swing_vwap_distance_pct <= -0.137658` 表示买入时价格至少低于动态 Swing VWAP 约 0.138%，卖出时价格至少高于动态 Swing VWAP 约 0.138%，即价格处在信号方向的回撤侧。

单指标按 20% 分位阈值扫描，并在六种退出轮廓上分别比较三个窗口的净期望。当前结果分为三类：

| 结论 | 指标或状态 | 结果 |
| --- | --- | --- |
| 稳定改善但不足以单独盈利 | `market_strength_state == strong` | 六种退出中五种在三个窗口同时改善；500% / 70% 平均提升 6.9 个百分点，但过滤后仍为 -2.8% |
| 有效辅助条件 | `adx14 <= 16.2934` | 中低趋势强度对中高止盈更有利；单独使用仍为负期望 |
| 有效辅助条件 | `atr_pct14 <= 0.203708` | 主要改善 500% 大波段捕获；单独使用的 500% / 70% 为 -4.2% |
| 有效辅助条件 | `dynamic_swing_vwap_distance_pct <= -0.137658` | 回撤侧入场改善多个退出轮廓，但单独使用的 500% / 70% 为 -4.5% |
| 弱改善 | `market_location_score <= 0`、较高市场强度与动量 | 多数窗口改善，但不能独立产生正期望 |
| 暂无稳定增量 | STC 区域、EMA alignment、原始方向 bias | 单字段差异小或跨窗口反向，不能据此设硬过滤 |

最值得继续验证的是低波动与回撤位置的交互，而不是某个单指标：

```text
atr_pct14 <= 0.203708
AND directional_dynamic_swing_vwap_distance_pct <= -0.137658
```

该组合保留 364 个原始信号，三个窗口样本数分别为 98、139、127；当前策略只放行其中 50 个，即只捕获 13.7%。这支持“有利润空间的信号没有被现有评分充分发掘”的判断。

| 止盈 / 止损 | 样本 | 止盈 | 止损 | 到期 | 正收益率 | 平均净收益 | 综合 PF | 最低窗口 PF | 三窗口平均净收益 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| 500% / 70% | 364 | 18 | 227 | 119 | 32.4% | +4.29% | 1.078 | 1.008 | +10.40% / +0.45% / +3.79% |
| 500% / 150% | 364 | 21 | 125 | 218 | 44.5% | +7.96% | 1.116 | 1.006 | +17.84% / +0.40% / +8.60% |

这是本轮唯一在三个窗口均达到正净期望和 Profit Factor 大于 1 的指标组合，但不能直接升级为策略规则：阈值来自同一批研究窗口，最弱窗口 PF 仅约 1.01，优势非常薄，且 500% 目标依赖少数大波段。它当前只能标记为 `Promising / requires OOS validation`。

ADX 与回撤位置也有明显交互。`adx14 <= 16.2934` 且处于动态 Swing VWAP 回撤侧时保留 202 个信号，在六种退出轮廓中全部跨窗口改善；100% / 100% 和 500% / 70% 的合并平均已转正，但仍有单个窗口为负，因此稳定性弱于上述低 ATR 组合。

当前数据不支持“指标越强越好”。相反，更有效的结构是：Supertrend 刚翻转时市场总体能力不弱，但短周期 ADX、ATR 不宜过高，入场价格应位于动态 Swing VWAP 的回撤侧。它描述的是低波动回撤后的潜在延续，而不是高波动追涨杀跌。

下一步必须固定上述两个候选组合，不再扫描阈值，并使用未参与本轮研究的新时间窗口做样本外验证；通过后才能考虑修改入场过滤。

## ETH 2025-08 至 2025-11 信号研究协议

下一阶段不执行策略仓位，只研究 ETHUSDT 3m 在 `2025-08-01T00:00:00Z` 至 `2025-12-01T00:00:00Z` 之间的全部 `supertrend_flip` 和 `wick_reclaim` 原始触发。每个信号相互独立，从信号后的下一根 K 线开始按时间回放，最长观察 12 小时。

每个信号同时计算固定保证金止损 `50% / 70% / 100% / 150%`、ATR 止损 `1.0 / 1.5 / 2.0 / 2.5 ATR`，以及保证金止盈阶梯 `30% / 50% / 75% / 100% / 150% / 200% / 300% / 500%`。杠杆口径固定为 100 倍，同时保存换算后的标的价格 bps。若同一根 K 线同时触及止盈和止损，保守地按止损处理；首次止损后停止观察，后续反弹不再属于该信号。

ClickHouse 使用两张研究表：

- `supertrend_signal_research`：每个原始信号一行，保存身份、方向、触发来源、入场价、ATR，以及入场时点的完整底层指标、窗口语义和多周期指标 JSON。
- `supertrend_signal_outcomes`：每个信号、止损模型和止盈阶梯一行，保存首次止盈、首次止损或到期结果，以及退出前 MFE、MAE、最高命中止盈阶梯和观察根数。

研究明细只保存在 ClickHouse；本文档后续只记录止盈止损矩阵、指标分布、稳定性检验和结论，避免把大体量逐信号数据写入 Markdown。训练区间内发现的阈值必须固定后再换区间验证，不能直接升级为策略规则。

### 2026-03 至 2026-06 样本外验证

使用 `2026-03-01T00:00:00Z` 至 `2026-07-01T00:00:00Z` 作为冻结样本外区间，继续观察 12 小时并使用相同的止盈止损枚举。该区间共产生 1,518 个 `supertrend_flip`，买卖各 759 个；冻结条件 `sr_position == mid AND wavetrend_momentum == weakening` 保留 101 个信号。没有在样本外数据上重新选择字段或阈值。

训练区间内表现较好的两个 300% 止盈组合均未通过样本外验证：

| 止损 | 样本 | 止盈命中 | 命中率 | 平均净收益 | Profit Factor |
| --- | ---: | ---: | ---: | ---: | ---: |
| 固定保证金 70% | 101 | 11 | 10.89% | -21.58% | 0.658 |
| 2.5 ATR | 101 | 11 | 10.89% | -9.87% | 0.798 |

固定 70% / 300% 相对未过滤样本没有改善，2.5 ATR / 300% 虽将平均净收益由 -17.81% 改善至 -9.87%，但仍明显亏损且 Profit Factor 小于 1。该候选组合应判定为训练区间拟合，不得进入正式策略。后续研究不能在该样本外区间上继续调参后仍把它称为样本外验证。

## 相关实现

- `backend/go-service/pkg/strategies/supertrend/supertrend.go`
- `backend/go-service/pkg/strategies/supertrend/supertrend_test.go`
- `backend/go-service/pkg/strategyregistry/registry.go`

## 趋势平台延续事件研究

2026-07-19 增加研究专用的 `trend_platform_breakout` 事件。该事件不进入在线 Supertrend 策略，不授权开仓，也不修改仓位或执行配置。研究检测器要求：

- 3m 与 5m Supertrend 方向一致；
- 收盘价同时位于 3m 与 5m Supertrend 价格线的趋势侧；
- 此前固定数量的 3m K 线形成窄幅平台；
- 当前收盘突破平台高点或低点；
- 当前成交量相对平台平均量达到最低倍数；
- 同方向事件在冷却窗口内只记录一次。

生命周期为 `inactive -> building -> ready -> breakout -> consumed/invalidated`。研究命令通过 `platform-window-bars`、`platform-max-range-pct`、`platform-min-volume-ratio` 和 `platform-cooldown-bars` 参数显式控制口径，事件参数和当时的 3m/5m Supertrend 状态写入 `trigger_metadata_json`。

ETHUSDT 2026-07-13 00:00 至 2026-07-19 07:30 的初次回放使用 12 根平台窗口、0.6% 最大振幅、1.5 倍成交量和 20 根冷却窗口，共记录 62 个平台事件，其中 53 个没有同时出现原始 Supertrend 翻转。该事件捕获了 2026-07-19 00:51 的平台后启动，但在 2 小时观察、固定 50% 保证金止盈、70% 保证金止损及 16% 往返成本口径下，多空平均净收益均为负。

因此当前结论是：平台延续属于有效的新事件观测能力，但现有四个条件不足以形成可交易入口。后续只能先做更长区间、first-passage 和样本外研究，不得直接接入策略授权。

### 最近七天多事件覆盖研究

同日继续增加两个研究事件，仍不进入在线策略：

- `volatility_impulse`：短窗口方向位移达到 ATR 倍数、收盘突破近期结构且 `volume_ratio20` 放大，用于捕获没有预先平台的急涨急跌。
- `trend_pullback_resume`：3m 与 5m Supertrend 同向、价格保持在两条 Supertrend 线趋势侧，回踩 EMA25 后在限定时间内重新突破短期结构且 `volume_ratio5` 恢复，用于捕获趋势二次启动。

2026-07-13 00:00 至 2026-07-19 07:30 的组合回放共保存 224 个研究信号。按截图中八段主要多空波段人工固定启动窗口，`supertrend_flip`、`trend_platform_breakout`、`volatility_impulse` 和 `trend_pullback_resume` 的联合事件在每段启动前后 45 分钟内均找到同方向候选；该结果只表示事件覆盖，不表示可交易收益。

| 事件 | 多头样本 | 空头样本 | 多头平均 MFE | 空头平均 MFE | 多头平均净收益 | 空头平均净收益 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `trend_platform_breakout` | 36 | 26 | 47.34 bps | 64.06 bps | -23.04% | -15.45% |
| `volatility_impulse` | 48 | 43 | 55.50 bps | 57.88 bps | -27.14% | -15.67% |
| `trend_pullback_resume` | 35 | 31 | 55.05 bps | 57.22 bps | -24.28% | -14.99% |

净收益使用 2 小时观察、固定 50% 保证金止盈、70% 保证金止损和 16% 往返成本。三类事件都提高了大波段覆盖，但候选数量和假动作同步增加，当前没有任何一类达到正净期望。因此联合覆盖结果不得解释为“最近七天八段行情都可以盈利交易”，下一阶段应研究事件去重、过度延伸、剩余空间和同类假信号标签，而不是继续增加入口。

### 跨来源同向去重

在同一根 3m K 线上先合并全部事件来源，再对合并后的买卖方向分别应用 20 根完整 K 线冷却。买卖方向的冷却互相独立；冷却只减少重复研究样本，不改变各检测器的事件定义，也不接入在线交易授权。

相同七天区间复跑后，研究信号由 224 个降至 127 个，减少 43.3%。八段人工固定波段仍全部在启动前后 45 分钟内找到同方向候选，最近事件距离人工启动点为 1 至 29 分钟。在相同 2 小时、50% 止盈、70% 止损和 16% 往返成本口径下，去重前后整体平均净收益均为 -21.16%。

因此 20 根冷却可以作为研究数据降噪规则：它在本区间保留 8/8 覆盖且没有降低平均结果，但没有改善负期望。现有入场特征在盈利与亏损候选之间高度重叠，当前证据不足以冻结“过度延伸”或“剩余空间”硬阈值；为避免在七天样本上追拟合，本轮不把这两项写成交易过滤器。

### 2026-05-01 至 2026-06-08 逆势门控消融

针对 ETHUSDT 从 2422.52 下跌至 1503.60、期间包含多轮反弹的行情，增加研究专用逆势门控：15m 或 30m 任一方向与信号一致时按顺势处理；两个周期都明确反向时，要求等待 3 根 3m K 线、5m Supertrend 已翻转、突破最近 3 根结构且 `volume_ratio20 >= 1.5`，每个高周期趋势阶段只允许一次，并在元数据中标记 25% 仓位因子。该逻辑不进入在线策略。

组合事件回放保存 534 个信号，其中顺势 512 个、逆势 22 个。九段人工固定多空波段仍全部有同方向候选，但首个候选延迟扩大至 96 至 643 分钟。使用固定 150% 止盈、150% 止损、30 分钟退出后冷却，并对逆势结果按 25% 因子缩放时，组合事件顺序回放为 104 笔，累计净结果 -4303.26%，最大回撤 4303.26%。

只保留与旧研究同源的 `supertrend_flip` 做消融时，新门控为 79 笔、累计净结果 -886.04%、最大回撤 1953.70%；未加该门控的基线为 106 笔、累计净结果 +1173.75%、最大回撤 826.99%。新门控虽然减少逆势尝试，却同时删除了过多有利的早期反转，并显著增加信号延迟，结论为失败，不得进入实盘。后续如继续研究，应使用独立样本先定义“高周期趋势衰竭”标签，而不是在本窗口继续调整等待根数、量比或结构阈值。

### 入场后 3 至 5 根 K 线有效性观察

同一区间增加研究观察表 `supertrend_signal_validation_observations`，对每个候选保存第 3、4、5 根 3m K 线时的 MFE、MAE、方向收盘收益、5m Supertrend 方向，以及收盘是否曾反穿信号 K 线高低点。观察能力与在线退出完全隔离，失败的逆势门控默认关闭。

本轮组合事件共有 796 个候选和 2388 条检查点观察。以固定 150% 止盈、150% 止损、退出后冷却 30 分钟为基线，枚举 3/4/5 根检查点和 10/15/20 bps 最低 MFE，并在原止盈止损尚未先触发时模拟提前退出。所有九种组合均为负；表现相对较好的 4 根/20 bps 组合仍累计 -2267.87%、最大回撤 2754.73%，同时救下 73 笔原止损并误杀 76 笔原盈利。

单条件消融同样未形成可靠规则：信号 K 线高低点过窄，结构条件对绝大多数顺序入选交易都会触发；5m 反向确认明显滞后；最低 MFE 条件误杀数不低于救回的止损数，例如 3 根/15 bps 救下 95 笔原止损但误杀 114 笔原盈利。结论是这些字段适合作为后验诊断观察，但不能直接用于提前退出。后续若继续，应先定义基于多根 K 线或 ATR 的宽结构失效区，并在独立窗口冻结定义，不能继续围绕本区间扫描阈值。

### 横盘状态识别研究

增加研究专用 `ChopDetector` 和 `supertrend_chop_observations` 观察表，不接入在线策略，也不直接禁止开仓。检测器以最近 60 根 3m K 线为观察窗，对以下五项证据投票：价格效率比、平台宽度/10m ATR、10m ADX、10m Supertrend 翻转次数、10m Supertrend 线的 ATR 归一化斜率。至少三票连续 10 根 3m K 线后进入 `chop_confirmed`；放量突破平台后进入 `breakout_pending`，连续两根保持在平台外才退出；横盘证据明显不足时也必须连续 5 根才退出，避免状态随单根 K 线来回切换。

ETHUSDT 2026-06-28 至 2026-07-15 回放共保存 8,580 条观察。按截图人工固定时段统计，`chop_watch`、`chop_confirmed` 与 `breakout_pending` 合计覆盖：6月29日磨损段 86.2%、7月3日平台 85.0%、7月10日平台 71.1%、7月11日震荡 87.2%、7月14日平台 49.0%；7月14日放量突破段 54 根全部为 `normal`。初版状态没有证据消失退出，7月8日真实下跌段曾有 60 根滞留在 `chop_confirmed`；增加 5 根退出迟滞后降至 35 根，但该趋势段仍有 17.5% K 线被确认成横盘，说明误杀尚未消除。

将同区间 368 个研究信号按状态联表后，直接过滤 `chop_confirmed` 和 `breakout_pending` 未显示正向增量。在固定 50% 止损、75% 止盈口径下，被过滤组 149 个候选的成本前平均结果为 +6.71 bps，允许组 217 个候选为 -4.49 bps；按 16 bps 往返成本换算，两组均为负，但被过滤组反而少亏。固定 150% / 150% 时结论相同：被过滤组成本前 +13.19 bps，允许组 +1.39 bps。横盘标签能描述人工截图中的多数盘整，却不能把“不可交易磨损”与“平台边缘反转/蓄势后延续”可靠分开。

因此当前只保留状态观察和诊断能力，不在策略入口增加硬过滤。下一步应固定定义后换独立区间验证，并增加平台内位置、突破失败次数与信号方向的交互；评价必须使用带手续费、单持仓和退出后冷却的顺序回放，不能用横盘覆盖率替代交易收益。
